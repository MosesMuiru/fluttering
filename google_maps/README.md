procedure
1. create a separe pages, for map page
2. google api key
3. checkout google_maps_flutter to see what the different properties are called 
4. add more markers on the google maps

location deps
- add the permission request on top of the apps
